services:
  arduino-builder:
    image: arduino-builder
    build:
      context: arduino-builder
    volumes:
      - type: bind
        source: ./CO2-Ampel_Plus
        target: /home/builder/CO2-Ampel_Plus
    environment:
      USER: ${USER}
